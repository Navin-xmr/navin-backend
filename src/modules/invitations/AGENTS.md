# invitations

**Route prefix:** `/api/company/invitations`

**Error prefix:** `ERR_INVITATIONS_<DESC>` (register in `src/shared/http/errors.ts`)

**Cross-module deps:**
- `src/modules/users/`

**Conventions:** See root `AGENTS.md` for all hard rules. Update this file manually if the module's dependencies or patterns change.
