# events

**Route prefix:** `/api/events`

**Error prefix:** `ERR_EVENTS_<DESC>` (register in `src/shared/http/errors.ts`)

**Cross-module deps:**
- `src/modules/infra/redis/`

**Conventions:** See root `AGENTS.md` for all hard rules. Update this file manually if the module's dependencies or patterns change.
